[package]
name = "runbook-render"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Render model builder for runbook UI snapshots."

[dependencies]
runbook-protocol = { path = "../runbook-protocol" }
