[package]
name = "runbook-hook-state"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Hook event to agent-state transition rules for Runbook."

[dependencies]
runbook-protocol = { path = "../runbook-protocol" }
