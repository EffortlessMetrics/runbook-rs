[package]
name = "runbook-agent-fsm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure Claude hook to agent-state transition logic for Runbook."

[dependencies]
runbook-protocol = { path = "../runbook-protocol" }
